---
question: "What are two reasons to keep an action in its own repository instead of bundling it with other application code? (Each correct answer presents a complete solution. Choose two.)"
documentation: "https://docs.github.com/en/actions/creating-actions/about-custom-actions"
---
- [ ] It makes the action.yml file optional.
- [x] It makes it easier for the GitHub community to discover the action.
- [ ] It widens the scope of the code base for developers fixing issues and extending the action.
- [x] It decouples the action’s versioning from the versioning of other application code.
- [ ] It allows sharing workflow secrets with other users.
