---
question: "When creating and managing custom actions in an enterprise setting, which of the following is considered a best practice?"
documentation: "https://docs.github.com/en/actions/creating-actions/about-custom-actions"
---
- [x] creating a separate repository for each action so that the version can be managed independently
- [ ] creating a separate branch in application repositories that only contains the actions
- [ ] creating a single repository for all custom actions so that the versions for each action are all the same
- [ ] including custom actions that other teams need to reference in the same repository as application code
